+ En el fichero "src/tourgune/mdp/log/DatabaseLogger.java" se han de modificar los parámetros por defecto de la base de datos donde logueará. Se han dejado como texto plano para que el editor de código lo detecte como error y no permita compilar y forzar así su reescritura.